<Window x:Class="InterlinedSync.UI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InterlinedList Sync Settings"
        Width="460" Height="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Settings"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <TextBlock Grid.Row="1" Text="Sync folder" Margin="0,0,0,4" />
        <TextBox Grid.Row="2"
                 Text="{Binding SyncFolder, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,12" />

        <TextBlock Grid.Row="3"
                   Text="{Binding PollIntervalSeconds, StringFormat='Poll interval: {0} seconds'}"
                   Margin="0,0,0,4" />
        <Slider Grid.Row="4"
                Minimum="{Binding MinimumPollIntervalSeconds, Mode=OneTime}"
                Maximum="300"
                Value="{Binding PollIntervalSeconds}"
                TickFrequency="10"
                IsSnapToTickEnabled="True"
                Margin="0,0,0,12" />

        <CheckBox Grid.Row="5"
                  Content="Launch InterlinedList Sync on Windows startup"
                  IsChecked="{Binding AutoStart}"
                  Margin="0,0,0,12" />

        <TextBlock Grid.Row="6"
                   Text="{Binding StatusMessage}"
                   Foreground="#555"
                   VerticalAlignment="Top" />

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close"
                    Width="80"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Click="CloseButton_OnClick" />
            <Button Content="Save"
                    Width="100"
                    IsDefault="True"
                    Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</Window>
